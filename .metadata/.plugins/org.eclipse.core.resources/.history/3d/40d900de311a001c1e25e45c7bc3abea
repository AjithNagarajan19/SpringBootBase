package controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class UserController {

	 @RequestMapping("/")
	    public String home(){
		 String return_Value = "";
		 int count = 1;
		/* if(count == 0){
			 return_Value = "True";
		 } else {
			 return_Value = "False";
		 }*/
		 
		 
		 String result = (count >= 16) ? "This user is over 16." : "This user is under 16.";
		 
	        return result;
	    }	
	
	
	
}
